---
title: "@anhur/core"
description: Config, schemas, and the anhur CLI.
sidebar:
  label: "@anhur/core"
  order: 1
---

The required package. Defines collections, validates documents, generates modules, and ships the **`anhur` CLI**.

## Install

```package-install
@anhur/core
```

The CLI (`anhur build`, `anhur watch`) is included — no separate package.

## Exports (typical)

- `defineConfig`, `defineCollection`, `defineSingleton`
- `schema as s`
- `getDocumentMeta`
- `defineIntegration`, `registerIntegration`
- Programmatic `build` / `watch`
- `formatAnhurError`

## Minimal config

```ts
import { defineCollection, defineConfig, schema as s } from "@anhur/core";

export default defineConfig({
  content: [
    defineCollection({
      name: "posts",
      directory: "content/posts",
      include: "**/*.md",
      schema: s.object({ title: s.string() }),
    }),
  ],
});
```

## Guides

- [Configuration](/guides/configuration)
- [Schemas](/guides/schemas)
- [CLI setup](/getting-started/cli)
- [CLI reference](/reference/cli)
