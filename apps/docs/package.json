{
  "name": "docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "blume dev",
    "build": "if [ -f .blume/dev.lock ]; then blume build --isolated; else blume build; fi",
    "preview": "blume preview",
    "check-types": "if [ -f .blume/dev.lock ]; then blume check --isolated; else blume check; fi",
    "doctor": "blume doctor",
    "validate": "blume validate"
  },
  "dependencies": {
    "astro": "^7.0.2",
    "blume": "^1.3.1"
  }
}
